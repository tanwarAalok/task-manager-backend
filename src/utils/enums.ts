export enum TaskStatus {
    TODO = 'todo',
    IN_PROGRESS = 'inprogress',
    UNDER_REVIEW = 'underreview',
    FINISHED = 'finished',
}

export enum TaskPriority {
    LOW = 'low',
    MEDIUM = 'medium',
    URGENT = 'urgent',
}